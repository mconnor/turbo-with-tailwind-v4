---
interface Props {
  className?: string;
}

const { className = 'bg-blue-500' } = Astro.props;
---

<div
  class:list={[
    'h-80 w-80 rounded-full mix-blend-multiply',
    { className: className },
  ]}
>
</div>
